import {WarCatClickGameComponent} from '../components';

type Props = any;

const Home = (props: Props) => {
  return (
    <>
      <WarCatClickGameComponent />
    </>
  );
};

export default Home;
